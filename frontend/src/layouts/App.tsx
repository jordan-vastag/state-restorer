import { Provider } from "@/components/ui/provider";
import { Navbar, Body, Footer } from "@/components";

function App() {
  return (
    <Provider>
      {/* <Navbar /> */}
      <Body />
      <Footer />
    </Provider>
  );
}

export default App;
